{% extends "base.html.twig" %}

{% block body %}

    {% block header %}
        <header class="app-header"><a class="app-header__logo" href="{{ path('main_dashboard') }}">Procost</a>
            <a class="app-sidebar__toggle" href="#" data-toggle="sidebar" aria-label="Hide Sidebar"></a>
            <!-- Navbar Right Menu-->
            <ul class="app-nav">
                <li class="app-search">
                    <a href="/logout" class="btn btn-info">Se déconnecter</a>
                </li>
            </ul>
        </header>
    {% endblock %}

    {% block sidebar %}
        {% include 'sidebar/_sidebar.html.twig' %}
    {% endblock %}

    <main class="app-content">
    {% block content %}{% endblock %}
    </main>

{% endblock %}